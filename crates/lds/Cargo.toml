[package]
name = "local-develop-server"
description = "Unified MCP server for the coding pipeline — consolidates git / recipe / sandbox tooling into one process with shared session state"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
keywords.workspace = true
categories.workspace = true
readme.workspace = true

[[bin]]
name = "lds"
path = "src/main.rs"

[dependencies]
lds-core = { path = "../core", version = "0.13.2" }
lds-git = { path = "../git", version = "0.13.2" }
lds-gh = { path = "../gh", version = "0.13.2" }
lds-recipe = { path = "../recipe", version = "0.13.2" }
lds-sandbox = { path = "../sandbox", version = "0.13.2" }
lds-journal = { path = "../journal", version = "0.13.2" }
lds-outline = { path = "../outline", version = "0.13.2" }
lds-publicity = { path = "../publicity", version = "0.13.2" }
lds-router = { path = "../router", version = "0.13.2" }
anyhow.workspace = true
serde.workspace = true
serde_json.workspace = true
tokio.workspace = true
tracing.workspace = true
tracing-subscriber.workspace = true
rmcp.workspace = true
schemars.workspace = true
clap.workspace = true
dirs.workspace = true
chrono = { version = "0.4", default-features = false, features = ["std"] }

[dev-dependencies]
tempfile.workspace = true
